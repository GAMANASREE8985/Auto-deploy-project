name: CI

on:
  push:
    branches:
      - main
  workflow_dispatch:

env:
  NODE_VERSION: 18
  BASE_DIR: 2025-26/Capstone_Project/auto-deploy-project

permissions:
  contents: read
  packages: write
  id-token: write

concurrency:
  group: ci-cd-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build:
    name: Build & Test
    runs-on: ubuntu-latest
    env:
      DOCKERHUB_USER: ${{ secrets.DOCKERHUB_USER }}
      DOCKERHUB_TOKEN: ${{ secrets.DOCKERHUB_TOKEN }}
      SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Show repo layout (debug)
        run: |
          echo "PWD: $(pwd)"
          echo "BASE_DIR = ${BASE_DIR}"
          ls -la
          if [ -d "${BASE_DIR}" ]; then
            ls -la "${BASE_DIR}"
          else
            echo "WARNING: BASE_DIR does not exist"
          fi

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: 'npm'

      - name: Cache backend node modules
        uses: actions/cache@v4
        with:
          path: ${{ env.BASE_DIR }}/backend/node_modules
          key: ${{ runner.os }}-backend-node-${{ hashFiles(format('{0}/backend/package-lock.json', env.BASE_DIR)) }}
          restore-keys: |
            ${{ runner.os }}-backend-node-

      - name: Backend - Install & Test
        working-directory: ${{ env.BASE_DIR }}/backend
        run: |
          npm ci
          npm test || true

      - name: Cache frontend node modules
        uses: actions/cache@v4
        with:
          path: ${{ env.BASE_DIR }}/frontend/node_modules
          key: ${{ runner.os }}-frontend-node-${{ hashFiles(format('{0}/frontend/package-lock.json', env.BASE_DIR)) }}
          restore-keys: |
            ${{ runner.os }}-frontend-node-

      - name: Frontend - Install & Test
        working-directory: ${{ env.BASE_DIR }}/frontend
        run: |
          npm ci
          npm test --watchAll=false || true

      - name: Build frontend production bundle (if exists)
        working-directory: ${{ env.BASE_DIR }}/frontend
        run: |
          if grep -q "\"build\"" package.json 2>/dev/null; then
            npm run build
          else
            echo "No build script found for frontend."
          fi

  docker_and_scan:
    name: Build & Push Docker Images + Scan
    needs: build
    runs-on: ubuntu-latest
    env:
      BASE_DIR: ${{ env.BASE_DIR }}
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Debug DOCKERHUB_USER (safe marker)
        run: |
          # Print markers to detect accidental spaces or leading slash.
          # This prints visible markers but not secrets themselves.
          echo "START_MARKER->${{ secrets.DOCKERHUB_USER }}<-END_MARKER"

      - name: Set up QEMU (optional multi-arch)
        uses: docker/setup-qemu-action@v3

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Login to DockerHub
        uses: docker/login-action@v2
        with:
          username: ${{ secrets.DOCKERHUB_USER }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Ensure backend Dockerfile exists (fail early)
        run: |
          echo "Checking backend Dockerfile: ${{ env.BASE_DIR }}/backend/Dockerfile"
          if [ ! -f "${{ env.BASE_DIR }}/backend/Dockerfile" ]; then
            echo "::error ::Backend Dockerfile missing at ${{ env.BASE_DIR }}/backend/Dockerfile"
            ls -la "${{ env.BASE_DIR }}/backend" || true
            exit 1
          fi

      - name: Build and push backend image
        uses: docker/build-push-action@v5
        with:
          context: ${{ env.BASE_DIR }}/backend
          file: ${{ env.BASE_DIR }}/backend/Dockerfile
          push: true
          tags: |
            ${{ replace(secrets.DOCKERHUB_USER, '/', '') }}/auto-deploy-backend:${{ github.sha }}
            ${{ replace(secrets.DOCKERHUB_USER, '/', '') }}/auto-deploy-backend:latest

      - name: Ensure frontend Dockerfile exists (fail early)
        run: |
          echo "Checking frontend Dockerfile: ${{ env.BASE_DIR }}/frontend/Dockerfile"
          if [ ! -f "${{ env.BASE_DIR }}/frontend/Dockerfile" ]; then
            echo "::error ::Frontend Dockerfile missing at ${{ env.BASE_DIR }}/frontend/Dockerfile"
            ls -la "${{ env.BASE_DIR }}/frontend" || true
            exit 1
          fi

      - name: Build and push frontend image
        uses: docker/build-push-action@v5
        with:
          context: ${{ env.BASE_DIR }}/frontend
          file: ${{ env.BASE_DIR }}/frontend/Dockerfile
          push: true
          tags: |
            ${{ replace(secrets.DOCKERHUB_USER, '/', '') }}/auto-deploy-frontend:${{ github.sha }}
            ${{ replace(secrets.DOCKERHUB_USER, '/', '') }}/auto-deploy-frontend:latest

      - name: Install Trivy
        run: |
          curl -sSfL https://raw.githubusercontent.com/aquasecurity/trivy/main/contrib/install.sh | sh -s -- -b /usr/local/bin

      - name: Trivy scan backend (CRITICAL,HIGH)
        run: |
          trivy image --severity CRITICAL,HIGH ${{ replace(secrets.DOCKERHUB_USER, '/', '') }}/auto-deploy-backend:${{ github.sha }} || true

      - name: Trivy scan frontend (CRITICAL,HIGH)
        run: |
          trivy image --severity CRITICAL,HIGH ${{ replace(secrets.DOCKERHUB_USER, '/', '') }}/auto-deploy-frontend:${{ github.sha }} || true

  sonar_scan:
    name: SonarQube (optional)
    needs: build
    runs-on: ubuntu-latest
    if: ${{ secrets.SONAR_TOKEN != '' }}
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
      - name: SonarQube placeholder
        run: |
          echo "Add Sonar scanner steps here (sonar-scanner CLI or action)."
